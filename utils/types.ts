export type heroImage = {
    src: string,
    label?: string,
    regHeight?: string
    mobileHeight?: string
    carousel?: boolean

}

export type navLinks = {
    href: string,
    label: string
}